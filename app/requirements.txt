# Coleta usa apenas a biblioteca padrão do Python para consultar a API do
# GitHub (urllib, json, argparse — ver README.md para justificativa). O único
# pacote de terceiros é o driver de banco, para conectar no Postgres (Supabase)
# compartilhado pelo time.
psycopg2-binary
pika>=1.3,<2
